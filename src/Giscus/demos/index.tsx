import { Giscus } from '@arietta-studio/ui';

export default () => {
  return (
    <Giscus
      id="demo"
      mapping="number"
      repo="arietta-studio/arietta-ui"
      repoId="" // TODO: Add repoId
      style={{ width: '100%' }}
      term="53"
    />
  );
};
